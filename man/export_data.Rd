% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-data.R
\name{export_data}
\alias{export_data}
\title{Export a flat dataset from a finished database}
\usage{
export_data(
  generation = c("refined"),
  source = NULL,
  db_dir = multised_db_dir(),
  out_dir = multised_analysis_dir(),
  verbose = TRUE
)
}
\arguments{
\item{generation}{Which database to export. Currently \code{"refined"} only.}

\item{source}{Must be \code{NULL}. Present for symmetry with \code{\link[=create_db]{create_db()}}; no
export is per-source yet.}

\item{db_dir}{Directory holding the databases. Defaults to
\code{\link[=multised_db_dir]{multised_db_dir()}}.}

\item{out_dir}{Directory the export is written under. Defaults to
\code{\link[=multised_analysis_dir]{multised_analysis_dir()}}.}

\item{verbose}{Print the summary as it runs.}
}
\value{
Invisibly, the paths of the files written.
}
\description{
Denormalises a pipeline database into a single flat, gzipped TSV plus a
column dictionary, for users who want the data without the relational
schema. It reads only, so re-running is always safe.
}
\details{
The refined export writes to \verb{out_dir/download/}, the path the
multised-refined site's pre-render step expects.
}
\examples{
\dontrun{
export_data("refined")
export_data("refined", out_dir = "~/sediment/exports")
}
}
