% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R, R/seastamp.R
\name{multised_db_dir}
\alias{multised_db_dir}
\alias{multised_analysis_dir}
\alias{multised_raw_dir}
\alias{multised_seastamp_dir}
\alias{multised_seastamp_bin}
\title{Where the databases, outputs and tools live}
\usage{
multised_db_dir()

multised_analysis_dir()

multised_raw_dir()

multised_seastamp_dir()

multised_seastamp_bin()
}
\value{
A length-one character path.
}
\description{
\code{multised_db_dir()} is the directory holding the SQLite databases,
\code{multised_analysis_dir()} the directory analysis outputs are written to,
\code{multised_raw_dir()} the vendor files the pilot stage parses, and
\code{multised_seastamp_dir()} the seastamp reference datasets (GSHHG, GEBCO, IHO,
Natural Earth, GISCO) the geo steps need. All default to the layout used by
the project itself and can be overridden globally with \code{\link[=options]{options()}}, or per
call via the \code{db_dir} and \code{seastamp_dir} arguments of \code{\link[=create_db]{create_db()}}.
}
\details{
\code{multised_seastamp_bin()} resolves the seastamp executable. seastamp is an
external dependency, so the normal case is an install the system can find on
the \code{PATH}, and that is what it looks for. Unlike the others it errors when
nothing is found, since a missing tool cannot be defaulted.
\code{getOption("multised.seastamp_bin")} and \code{Sys.getenv("SEASTAMP_BIN")} take
precedence over the \code{PATH}, for RStudio sessions: its console does not
inherit the login shell's \code{PATH}, so a binary the Terminal tab finds may
still be invisible to the console.
}
\examples{
multised_db_dir()

\dontrun{
options(multised.db_dir = "~/sediment/db")
}
}
