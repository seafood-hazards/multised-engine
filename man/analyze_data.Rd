% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze-data.R
\name{analyze_data}
\alias{analyze_data}
\title{Run the analyses for a generation}
\usage{
analyze_data(
  generation = c("clean", "merged", "refined"),
  module = NULL,
  steps = NULL,
  db_dir = multised_db_dir(),
  out_dir = multised_analysis_dir(),
  verbose = TRUE
)
}
\arguments{
\item{generation}{Which database to read: \code{"clean"} (per source),
\code{"merged"} or \code{"refined"}.}

\item{module}{Optional module name, for example \code{"grainsize"}. \code{NULL} runs
every module for the generation. See \code{\link[=analysis_modules]{analysis_modules()}}.}

\item{steps}{Optional subset of step numbers within a module. Only
\code{"background"} has more than one step.}

\item{db_dir}{Directory holding the databases. Defaults to
\code{\link[=multised_db_dir]{multised_db_dir()}}.}

\item{out_dir}{Directory the CSVs are written under. Defaults to
\code{\link[=multised_analysis_dir]{multised_analysis_dir()}}.}

\item{verbose}{Print each analysis's console summary as it runs.}
}
\value{
Invisibly, a named list with one element per module run, holding the
paths of the files in that module's output directory.
}
\description{
Analyses read a finished database and write tidy CSVs under
\verb{out_dir/<module>/}, one directory per module. They never modify a pipeline
database, so re-running is always safe.
}
\details{
The \code{outlier_review} module is a review prototype rather than a site input:
it settled the outlier rule the merge stage now applies, and writes candidate
CSVs and distribution plots for eyeballing. It is included in a full
\code{"merged"} run, and needs the suggested package ggplot2.
}
\examples{
\dontrun{
# every analysis for the merged database
analyze_data("merged")

# one module
analyze_data("clean", module = "grainsize")

# re-run only the enrichment-factor step of the background suite
analyze_data("refined", module = "background", steps = 4)
}
}
