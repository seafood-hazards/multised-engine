% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-db.R
\name{create_db}
\alias{create_db}
\title{Build a database generation}
\usage{
create_db(
  generation = c("pilot", "slim", "clean", "merged", "refined", "aquaculture"),
  source = NULL,
  steps = NULL,
  db_dir = multised_db_dir(),
  seastamp_dir = multised_seastamp_dir(),
  seastamp_bin = multised_seastamp_bin(),
  verbose = TRUE
)
}
\arguments{
\item{generation}{The generation to build: \code{"pilot"}, \code{"slim"}, \code{"clean"},
\code{"merged"} or \code{"refined"}, or \code{"aquaculture"} for the reference database.}

\item{source}{For the per-source generations, one of \code{\link[=multised_sources]{multised_sources()}}.
Must be \code{NULL} for \code{"merged"}, \code{"refined"} and \code{"aquaculture"}.}

\item{steps}{Optional subset of step numbers, for example \code{steps = 3:12}.
Steps 1 and 2 form one unit (the parse and the write), so requesting either
runs both. Defaults to every step that applies to the source.}

\item{db_dir}{Directory holding the databases. Defaults to
\code{\link[=multised_db_dir]{multised_db_dir()}}.}

\item{seastamp_dir}{Directory holding the seastamp reference datasets, used by
the two geo steps (pilot step 4 and clean step 4) and ignored by every
other generation. Defaults to \code{\link[=multised_seastamp_dir]{multised_seastamp_dir()}}.}

\item{seastamp_bin}{Path to the seastamp executable, used by the same two
steps. Defaults to \code{\link[=multised_seastamp_bin]{multised_seastamp_bin()}}, which reads the
\code{multised.seastamp_bin} option, then \code{SEASTAMP_BIN}, then the \code{PATH}.}

\item{verbose}{Print each step's summary as it runs.}
}
\value{
Invisibly, a named list with one element per step run, holding that
step's summary.
}
\description{
Runs a pipeline generation end to end. The first three generations
(\code{"pilot"}, \code{"slim"}, \code{"clean"}) produce one database per source and require
\code{source}; the last two (\code{"merged"}, \code{"refined"}) combine all five and must not
be given one.
}
\details{
\code{"aquaculture"} is not a pipeline generation. It builds a standalone Norway-only
reference database of marine aquaculture sites from the Fiskeridirektoratet
exports, which clean step 5 measures \code{site.dist_to_aquaculture} against. Build
it once, before the clean generation needs it.

Every step is idempotent, so re-running the whole generation, or a subset via
\code{steps}, is safe.
}
\examples{
\dontrun{
# the whole slim generation for one source
create_db("slim", "mareano")

# re-run only the grain-size steps
create_db("slim", "ices-dome", steps = 14:15)

# against databases held somewhere else
create_db("slim", "mudab", db_dir = "~/sediment/db")

# geo-enrich from a reference tree outside the project
create_db("pilot", "mareano", seastamp_dir = "~/seastamp")

# skip the geo step where seastamp is not installed
create_db("pilot", "mareano", steps = c(1, 5))

# the aquaculture reference database, which clean step 5 needs
create_db("aquaculture")
}
}
