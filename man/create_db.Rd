% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-db.R
\name{create_db}
\alias{create_db}
\title{Build a database generation}
\usage{
create_db(
  generation = c("pilot", "slim", "clean", "merged", "refined"),
  source = NULL,
  steps = NULL,
  db_dir = multised_db_dir(),
  verbose = TRUE
)
}
\arguments{
\item{generation}{The generation to build: \code{"pilot"}, \code{"slim"}, \code{"clean"},
\code{"merged"} or \code{"refined"}.}

\item{source}{For the per-source generations, one of \code{\link[=multised_sources]{multised_sources()}}.
Must be \code{NULL} for \code{"merged"} and \code{"refined"}.}

\item{steps}{Optional subset of step numbers, for example \code{steps = 3:12}.
Steps 1 and 2 form one unit (the parse and the write), so requesting either
runs both. Defaults to every step that applies to the source.}

\item{db_dir}{Directory holding the databases. Defaults to
\code{\link[=multised_db_dir]{multised_db_dir()}}.}

\item{verbose}{Print each step's summary as it runs.}
}
\value{
Invisibly, a named list with one element per step run, holding that
step's summary.
}
\description{
Runs a pipeline generation end to end. The first three generations
(\code{"pilot"}, \code{"slim"}, \code{"clean"}) produce one database per source and require
\code{source}; the last two (\code{"merged"}, \code{"refined"}) combine all five and must not
be given one.
}
\details{
Every step is idempotent, so re-running the whole generation, or a subset via
\code{steps}, is safe.
}
\examples{
\dontrun{
# the whole slim generation for one source
create_db("slim", "mareano")

# re-run only the grain-size steps
create_db("slim", "ices-dome", steps = 14:15)

# against databases held somewhere else
create_db("slim", "mudab", db_dir = "~/sediment/db")
}
}
