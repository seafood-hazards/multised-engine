% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seastamp.R
\name{seastamp_enrich}
\alias{seastamp_enrich}
\title{Annotate coordinates with location attributes}
\usage{
seastamp_enrich(
  points,
  id_col = "site_id",
  lon_col = "longitude",
  lat_col = "latitude",
  seastamp_dir = multised_seastamp_dir(),
  work_dir = file.path(tempdir(), "seastamp"),
  region = "auto",
  verbose = TRUE
)
}
\arguments{
\item{points}{A data frame with an id column and longitude/latitude columns.}

\item{id_col, lon_col, lat_col}{Column names in \code{points}.}

\item{seastamp_dir}{Directory holding the reference datasets.}

\item{work_dir}{Scratch directory for the intermediate TSVs.}

\item{region}{seastamp's projection region, \code{"auto"} by default: the
projection is derived from the points themselves, which is seastamp 0.12.0's
own default and the accurate choice.

Pass \code{region = "global"} to reproduce the values stored by the pre-0.9.0
\code{geoenrich} builds. Measured difference between the two over the 27,015
clean sites: \code{dist_to_coast} moves on every row (median 4.8-11.3\% by
source, largest single shift 93 km), \code{municipality} is reassigned for 1,884
sites and \code{country} for 283, while \code{depth} is unchanged everywhere and
\code{sea_name} changes for 2 sites. \code{depth} does not project, so it cannot
depend on this.}

\item{verbose}{Print progress.}
}
\value{
A data frame with the id column plus \code{depth}, \code{country},
\code{country_code}, \code{dist_to_coast}, \code{municipality} and \code{sea_name}.
}
\description{
Runs the external \code{seastamp} command-line tool over a table of points and
returns the location attributes for each: distance to coast, sea-floor depth,
sea name, country, country code and municipality.
}
\details{
This is the single location-annotation path for the whole pipeline. It needs
the \code{seastamp} binary and its reference datasets (GSHHG, GEBCO, IHO, Natural
Earth, GISCO); neither ships with the package.
}
\examples{
\dontrun{
pts <- data.frame(site_id = 1:2,
                  longitude = c(5.32, 10.75),
                  latitude  = c(60.39, 59.91))
seastamp_enrich(pts)

# adopt seastamp's own, more accurate default instead
seastamp_enrich(pts, region = "auto")
}
}
